{
  "name": "failure-avoidance-routing-agent",
  "version": "1.0.0",
  "private": true,
  "description": "Deterministic testnet USDC routing agent that remembers failed routes.",
  "scripts": {
    "demo": "node src/demo.js"
  }
}
